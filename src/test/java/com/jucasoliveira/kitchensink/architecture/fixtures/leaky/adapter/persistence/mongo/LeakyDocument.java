package com.jucasoliveira.kitchensink.architecture.fixtures.leaky.adapter.persistence.mongo;

/** A persistence-adapter type for the other fixtures to reach for. Violates nothing on its own. */
public record LeakyDocument(String id) {
}
